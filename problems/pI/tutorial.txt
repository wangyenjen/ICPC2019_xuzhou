令$c_i=max(b_j), 1\le j\le n \space \and j\neq i$

首先考慮沒有物品被刪的情況。對於Amy來說，其選物品$i$的結果為$a_i-c_i$，因此Amy會挑$i$滿足$a_i-c_i$的最大值。

而此$i$只有兩種可能，第一種是$a_i$最大，第二種是$b_i$最大。

接下來考慮Bob會移除掉些東西，假設現在要刪第$j$個物品，可以發現若Bob沒有移除Amy所挑選的$i$，則答案永遠不會變小，因此只要每次將Amy所挑選的$i$移除，且與$ans_{j-1}$取min即可(有可能挑掉$i$後值變大，此時當作先挑掉其他物品而不是取$i$)。